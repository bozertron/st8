{
  "batch": "indexing-engine",
  "generatedAt": "2026-05-14",
  "description": "indexer.js (the file indexing engine) + notificationBus.js (event bus). indexer.js carries the loadLibModule pattern again with 4 dynamic loads — all 4 target files have already moved, so the patch is to retarget each loader at its new home using direct static requires.",
  "moves": [
    { "from": "backend/indexer.js",         "to": "src/features/indexing/indexer.js" },
    { "from": "backend/notificationBus.js", "to": "src/core/notification-bus.js" }
  ]
}
