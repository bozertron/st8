# Research: CLAUDE.md — T4 scope analysis

**Wave:** analysis-tools-unblock-pass-1
**Ticket:** T4 — Wire route-manifest → CLAUDE.md + README API table generator + drift test
**Date:** 2026-05-16
**Mode:** read-only research; no edits proposed beyond T4 scope

---

## 1. Identity

- Path: `/home/user/st8/CLAUDE.md`
- LOC: **142** (verified `wc -l` → 142)
- Last (and only) touching commit: **`5a3264a`** "docs: add CLAUDE.md + PRE_FLIGHT.md — drift-prevention infrastructure" (2026-05-16 06:05:34 +0000)
- `git log --all -- CLAUDE.md` returns exactly one commit. The file is new — it has not been edited since creation.

## 2. Section inventory

| Section | Line range | Hand-written or could-be-derived | Drift risk |
|---|---|---|---|
| Title + "read this first" | 1–3 | Hand-written, evergreen | none |
| The real refactored tree (canonical) | 5–34 | Hand-written ASCII tree of `src/core`, `src/features`, `src/shared`, `src/frontend` | low — `ls src/` is stable post-refactor; new subdirs would drift |
| Entry-point chain | 36–51 | Hand-written; embeds **LOC counts** (`start.js (148 LOC)`, `main.js (453 LOC)`, `app.js (2264 LOC)`) | medium — LOC values drift on every edit. `find` confirms current `wc -l`: `start.js`=148, `main.js`=453, `app.js`=2364 — **`app.js` already drifted from 2264 → 2364** (verified `wc -l src/core/server/app.js` is out of scope, but cross-referenced via README-md.md research and meta-dogfood). LOC numbers in the chain are stale. |
| Tests | 53–59 | Hand-written; embeds count "**207 passing**" | high — meta-dogfood (`docs/_pending-tickets/meta-dogfood.md:5`) records "451 / 451 pass" baseline; CLAUDE.md's "207" is severely stale |
| Authoritative file inventory | 61–63 | Pointer-only ("see `st8-filemap.md`, auto-generated by `docs/generate-filemap.js`") | none — meta-deferred to a real generator |
| HISTORICAL ARTIFACTS — DO NOT WORK ON | 65–74 | Hand-written but historical-fact (commit shas `36d9c16` / `d340af4`); evergreen by construction | none |
| Hooks / signal flow | 76–84 | Hand-written list of `HOOKS` constants + behavior | low — could-be-derived from `src/core/hook-registry.js` exports, but it changes rarely |
| Persistence invariants (post-Wave-1) | 86–93 | Hand-written | low — these are architectural commitments, not enumerations |
| Identity invariants (post-Wave-3A) | 95–100 | Hand-written | low |
| **API surface (post-Wave-3B + 3C)** | **102–118** | **Hand-written 11-row markdown table — duplicates `src/core/server/route-manifest.js` (33 entries) — T4 target** | **high** |
| Active sprint protocol | 120–128 | Hand-written | low — references the "23-wave sprint" generically; sprint structure stable |
| NEVER target these paths | 130–135 | Hand-written | low |
| When in doubt | 137–142 | Hand-written troubleshooting commands | none — commands are evergreen |

## 3. The API surface table (lines 102–118)

### Header / shape
- Line 102: `## API surface (post-Wave-3B + 3C)`
- Line 104: `| Route | Method | Purpose | Auth |`
- Line 105: separator `|---|---|---|---|`
- Data rows: **11** (lines 106–116, confirmed `grep -c "^| \`/api" CLAUDE.md` → 11)
- Footer paragraph (line 118): describes `X-St8-Secret`, `.st8/server.secret`, `window.st8AuthFetch` — load-bearing prose, not part of the table proper; should remain hand-curated outside the sentinel region.

### Row-by-row reconciliation against `src/core/server/route-manifest.js`

route-manifest.js declares **33 entries** (`grep -c "^  {" route-manifest.js` → 33). Comparison:

| CLAUDE.md row | In route-manifest? | Notes |
|---|---|---|
| `/api/state` GET | **NO** | Endpoint does not exist (confirmed by meta-dogfood:13, 21–23 — returns 404 "API endpoint not found"). **Aspirational/fiction.** |
| `/api/manifests` GET | **NO** | Endpoint does not exist (meta-dogfood:13, 24–25 — 404). Closest working route is `/api/connection-state.json`. **Fiction.** |
| `/api/events` GET (SSE) | **NO** (under that name) | Manifest declares `/api/mutations` GET as the SSE stream (`route-manifest.js:80`). CLAUDE.md row uses a name that never existed. |
| `/api/tickets` GET / POST | YES | manifest line 137; auth label "POST: X-St8-Secret" semantically matches `auth: 'X-St8-Secret'` |
| `/api/tickets/count` GET | YES | manifest line 140 |
| `/api/record-commit` POST | YES | manifest line 134, `auth: 'X-St8-Secret'` |
| `/api/auth-token` GET | YES | manifest line 151, `auth: 'loopback'` |
| `/api/signal-path` GET / POST | YES | manifest line 159, `auth: 'none'` |
| `/api/generate-report` POST | YES | manifest line 162, `auth: 'none'`. (See meta-dogfood:14, 68–71 — endpoint hangs the event loop, but it IS in the manifest.) |
| `/api/insights` GET | YES | manifest line 165 |
| `/api/identity-risk` GET | YES | manifest line 168 |

**Coverage delta:** CLAUDE.md lists 11 rows; 8 correspond to real manifest entries (+1 with renamed `/api/events` vs `/api/mutations`), 3 are fictional (`/api/state`, `/api/manifests`, and `/api/events` under that name). The manifest has **22 additional** `/api/*` routes (the indexer/lifecycle/PRD/Bruno-Oscar/templates/settings/llm-call/exec/templates/ai-review/file-list cluster) the CLAUDE.md table omits entirely.

**Net:** CLAUDE.md API table is ~25% accurate against the canonical manifest. T4 is the right vehicle to fix it; the meta-dogfood report (`docs/_pending-tickets/meta-dogfood.md:169`) flags this exact drift as a residualConcern.

(Note on the ticket-prompt's "30 entries" / "12 rows" probe — current truth is 33 manifest entries + 11 CLAUDE.md rows; the prompt is approximately right but stale by 1–3 on each side.)

## 4. Other doc-drift hazards (NOT in T4 scope)

These sections have drift but no generator candidate — flag for separate tickets, do not touch in T4:

1. **Entry-point chain LOC (lines 39, 42, 46)** — `start.js (148 LOC)` and `main.js (453 LOC)` appear current; `app.js (2264 LOC)` is stale (current is ≈2364 per cross-references in other research; even the `_handleApiRequest` body keeps growing). A generator that injects `wc -l` for these three files inside a sentinel region would solve this — separate ticket, not T4.

2. **Tests "207 passing" (line 57)** — meta-dogfood records "451 / 451" baseline. This is one of the most-cited numbers in the doc (it functions as a load-bearing health gauge). A generator that runs `node --test` (or reads `tests/.test-count` cache) and emits the number under a sentinel would fix it — separate ticket.

3. **Authoritative file inventory pointer (line 63)** — points at `st8-filemap.md` and `docs/generate-filemap.js`. **Already a generator-managed artifact.** No drift here; the pattern referenced here is the precedent T4 should follow.

4. **Hooks list (lines 78–82)** — 9 hook constants declared. Could be derived from `src/core/hook-registry.js`'s `HOOKS` export. Low drift in practice; not T4.

5. **Persistence invariants (lines 86–93)** — narrative claims (`PRAGMA foreign_keys = ON`, `EXPECTED_SCHEMA`, `getSharedPersistence()` singleton). Verifiable but no clean generator hook. The meta-dogfood:165 finding "Insights not persisted … CLAUDE.md 'post-Wave-1 persistence invariants' section claims an insight store but it's smoke" is an accuracy concern but content-shaped, not table-shaped. **Out of T4 scope; route to its own ticket** (see also `src_features_analysis_insight-store.md:156, 196, 198, 235`).

6. **Wave references** — "post-Wave-3B + 3C" (line 102), "post-Wave-1" (line 86), "post-Wave-3A" (line 95) — Wave structure is documented in `st8_bible.md` (line 122 pointer). No drift detector; will quietly age. Not T4.

7. **The 4 static-asset references** that exist in README (`/`, `/*.js`, `/*.css`, `/*.ttf`) are NOT present in CLAUDE.md — CLAUDE.md is `/api/*`-only already, which is the same scope T4's generator should emit. **CLAUDE.md is the cleaner target of the two.**

## 5. Prior work touching CLAUDE.md

Exactly one commit ever touches the file:

- `5a3264a` (2026-05-16) "docs: add CLAUDE.md + PRE_FLIGHT.md — drift-prevention infrastructure" — the file was born today (same day as this research).

No ticket has executed against CLAUDE.md. No issue, sub-issue, or sprint review has modified it. The file is brand-new, ironically created as drift-prevention infrastructure while itself containing the largest hand-written drift surface in the repo (the API table).

## 6. Existing tests

None directly assert on CLAUDE.md content.

- `tests/core/server/route-manifest-drift.test.js` (153 LOC) — asserts manifest ↔ `app.js` 1:1; **does NOT consult CLAUDE.md or README.md**. Pattern is the precedent for T4's drift test.
- `grep -rn "CLAUDE.md" tests/` returns no matches.
- `grep -rn "CLAUDE.md" src/` returns one prose mention in `src/core/server/app.js:2324` (a JSDoc reference to "CLAUDE.md's 'no publisher without a real subscriber' rule"); not a test, not a consumer of CLAUDE.md contents.
- `grep -rn "CLAUDE.md" scripts/` — no matches.
- `grep -rn "CLAUDE.md" docs/` — references are all in `docs/_research/` (this folder), `docs/_pending-tickets/meta-dogfood.md`, and `docs/_pending-roadmap/mvp-polish-and-shipping.md`.

No `README` / `CLAUDE` drift / link-check / generator test exists today.

## 7. Contracts

CLAUDE.md is **auto-loaded into every Claude session in this repo as `claudeMd` system context** (visible in the running session-system-reminder: "Contents of /home/user/st8/CLAUDE.md (project instructions, checked into the codebase)" with the note "These instructions OVERRIDE any default behavior and you MUST follow them exactly as written").

Implications:
- **Every** Claude-driven edit, ticket execution, and review reads this file at the top of the context. Stale content propagates as wrong instructions to agents.
- The "API surface" table is what an agent will reach for when asked "does st8 have an endpoint for X?" — fictional rows (`/api/state`, `/api/manifests`, `/api/events`) cause agents to fabricate consumer code that 404s in production.
- The `HISTORICAL ARTIFACTS` and `NEVER target these paths` sections are protective: they prevent agents from re-introducing `backend/`, `lib/`, `src/0_*`, `OGB/` paths. **Must not be touched by T4.**
- The Entry-point chain (lines 36–51) is referenced by other research (e.g. README-md.md:138) as the canonical description for P0.3 to copy. **T4 must preserve this section verbatim.**

T4's surface is limited to the **API surface table only** (lines 102–116, possibly extending to 118 if the footer prose is left outside the sentinel, which it should be).

## 8. Change vector for T4

### 8a. Sentinel placement

Wrap **lines 104–116** (the header row + separator + 11 data rows) with sentinels. Leave line 102 (section heading `## API surface (post-Wave-3B + 3C)`) and line 118 (footer prose about `X-St8-Secret`) **outside** the sentinel region so the generator only owns the table body.

Recommended sentinel comments (HTML-style, matches the convention proposed in `README-md.md:107` so both files use the same markers):

```
<!-- BEGIN: route-manifest-table -->
| Route | Method | Auth | Description |
|---|---|---|---|
| `/api/connection-state.json` | GET | none | Read live manifest JSON … |
| `/api/ai-signal.toml` | GET | none | Read live TOML manifest … |
| `/api/health` | GET | none | { status, uptime, targetDir, lastManifestUpdate }. |
| `/api/index` | POST | none | Re-index a path; writes manifests, returns file count. … |
| `/api/file-intent` | POST | none | Upsert {purpose, dependsOnBehavior, valueStatement} … |
| `/api/verify` | POST | none | Walk indexed files, recompute SHA-256 … |
| `/api/files` | GET | none | List directory entries at ?path=. … |
| `/api/mutations` | GET | none | Server-sent events stream of mutation notifications. |
| `/api/concept-file` | POST | none | Phase-6: register a CONCEPT-phase file … |
| `/api/mvp-lock` | POST | none | Transition every CONCEPT/DEVELOPMENT file to LOCKED … |
| `/api/production-promote` | POST | none | Purge development-only mutation history … |
| `/api/prd` | GET | none | Generate the cross-card PRD markdown … |
| `/api/gap-analysis` | GET | none | Run GapAnalyzer over schema cards … |
| `/api/prd-projects` | GET / POST | none | List or create PRD project records. … |
| `/api/prd-projects/:name` | GET | none | Fetch a single PRD project by name … |
| `/api/bruno-call` | POST | none | Run BrunoOscar.runBrunoCall(threshold) … |
| `/api/oscar-house` | POST | none | Run BrunoOscar.runOscarHouse(gracePeriod) … |
| `/api/needs-ai-review` | GET | none | List files flagged needsAIReview. |
| `/api/mark-reviewed` | POST | none | Mark a file as reviewed by AI … |
| `/api/templates` | GET / POST | none | GET lists PRD templates; POST saves … |
| `/api/settings` | GET / POST | none | GET returns settings; POST upserts … |
| `/api/record-commit` | POST | X-St8-Secret | Receive post-commit hook payload … |
| `/api/tickets` | GET / POST | X-St8-Secret | GET lists 200 newest open tickets (no auth); POST creates … |
| `/api/tickets/count` | GET | none | { count } of open tickets — phreak> TUI badge source. |
| `/api/tickets/:id/claim` | POST | X-St8-Secret | Claim a ticket by id … |
| `/api/tickets/:id/resolve` | POST | X-St8-Secret | Resolve a ticket by id … |
| `/api/auth-token` | GET | loopback | Return the shared X-St8-Secret value to a loopback caller … |
| `/api/llm-call` | POST | X-St8-Secret | Dispatch a chat-completion to a configured provider. |
| `/api/signal-path` | GET / POST | none | Path-generator (FOUNDER P1). |
| `/api/generate-report` | POST | none | Report-generator output as markdown or JSON. |
| `/api/insights` | GET | none | Insight-store consumer. |
| `/api/identity-risk` | GET | none | Identity-risk consumer (Wave 3C). |
| `/api/exec` | POST | X-St8-Secret | Deliberately deferred — returns 501 with roadmap pointer. |
<!-- END: route-manifest-table -->
```

Row-shape decisions encoded above (resolving README-md.md §10 questions):
- Columns: `Route | Method | Auth | Description` (4 cols). Existing CLAUDE.md is `Route | Method | Purpose | Auth` (Purpose-before-Auth). Recommend swap to `Route | Method | Auth | Description` to match the manifest field order (`method, path, handler, auth, description`) and reduce special-casing in the generator. Either order is defensible; pick one and apply to README too.
- Multi-method rows: `GET / POST` (matches existing CLAUDE.md style at lines 109, 113).
- Auth column: emit manifest's literal string verbatim (`none` / `X-St8-Secret` / `loopback`). No translation.
- Description: take from `r.description`. Long descriptions may be truncated with `…` at first sentence boundary (≤120 chars) to keep the table scannable — generator choice; document in the generator's header comment.

### 8b. Other CLAUDE.md content T4 must NOT touch

Per the founder note in the prompt: "**DO NOT propose hand-edits to the file outside the API table region — the rest is foundation.**" Specifically out of scope:

- Lines 5–34 canonical tree
- Lines 36–51 entry-point chain (including the stale `app.js (2264 LOC)` — flag, don't fix)
- Lines 53–59 tests count "207 passing" (stale, flag, don't fix)
- Lines 61–63 file inventory pointer
- Lines 65–74 HISTORICAL ARTIFACTS
- Lines 76–84 hooks
- Lines 86–93 persistence invariants
- Lines 95–100 identity invariants
- Lines 120–128 active sprint protocol
- Lines 130–135 NEVER target paths
- Lines 137–142 when in doubt

The "rest is foundation" guidance is absolute. Flag any drift surfaced above (LOC, test count, persistence-invariants-vs-meta-dogfood) as **separate tickets**:

- Sub-T4.a (separate ticket): LOC sentinel for entry-point chain (`start.js`, `main.js`, `app.js`).
- Sub-T4.b (separate ticket): test count sentinel.
- Sub-T4.c (separate ticket): persistence-invariants accuracy review per meta-dogfood:165.

None of these go into T4's commit.

### 8c. Is there a route-count number elsewhere in CLAUDE.md that needs wrapping?

**No.** Searched the file for any numeric count of routes (e.g. "13 routes", "11 endpoints", etc.) — none. The table itself is the only enumeration. Wrapping just the table body is sufficient; no secondary sentinel needed for a count statement.

(`route-manifest.js:33` does mention "~26" and "~30" thresholds in its header comment as architectural guidance, but those are in the source file, not in CLAUDE.md.)

### 8d. Generator + test placement

Following `README-md.md §8`:
- Generator: `docs/generate-api-table.js` (sibling of existing `docs/generate-filemap.js`).
- Generator behavior: `require('../src/core/server/route-manifest')`; emit markdown table; rewrite the sentinel-bounded region in **both** `README.md` and `CLAUDE.md`.
- Drift test: `tests/docs/api-table-drift.test.js`. Pattern mirrors `tests/core/server/route-manifest-drift.test.js`: read both files, extract sentinel-bounded region, regenerate from manifest, `assert.strictEqual`. CI fails if either file's region diverges.
- Optionally: an `npm run docs:api-table` script in `package.json` to regenerate (currently `package.json` has `test`, `start`, `index`, `serve`, `dev` — no docs script; not strictly required for T4 but a convention worth following).

## 9. Provisions already made

- **No existing sentinel comments in CLAUDE.md.** `grep -n "BEGIN:" CLAUDE.md` returns nothing. Greenfield region.
- **No TODO/FIXME markers in CLAUDE.md.** `grep -n "TODO\|FIXME\|XXX" CLAUDE.md` returns nothing. The file is in its first-day state.
- **`src/core/server/route-manifest.js` exists** (180 LOC, 33 entries, `Object.freeze`-d) as the declared contract. Its header (lines 11–14) already names "Documentation (cluster docs in `docs/components/`)" as a consumer — T4 extends that consumer set to CLAUDE.md + README.md.
- **`tests/core/server/route-manifest-drift.test.js` already enforces manifest ↔ `app.js`** so the generator can trust the manifest is a faithful reflection of the actual switch in `app.js`.
- **`docs/generate-filemap.js` precedent**: referenced in CLAUDE.md:63 itself ("auto-generated by `docs/generate-filemap.js`"). Same pattern, sibling location.
- **README sentinel proposal** in `docs/_research/2026-05-16-analysis-tools-unblock/README-md.md` (lines 105–127) already specifies the same sentinel scheme. T4 lands them in lockstep.

## 10. Gaps + open questions

1. **Is there a pre-commit hook that would block edits to CLAUDE.md from a Claude session?** No. `ls .git/hooks/` shows only `.sample` files (no active hooks installed). No `.husky/`. `package.json` has no `husky` / `lint-staged` config. CI is not configured visibly in the repo (no `.github/workflows/`, no `.gitlab-ci.yml`). **A Claude session that hand-edits CLAUDE.md mid-conversation will not be blocked at commit time** — the only line of defense T4 adds is the drift test, which runs only when someone invokes `npm test`. Recommend the T4 drift test be the gate; if someone wants stronger enforcement, a pre-commit hook can wrap `npm test -- tests/docs/` as a follow-up (out of T4 scope).

2. **What happens if a sprint executor agent re-generates a stale CLAUDE.md as a side effect?** The drift test will fail until the agent re-runs `node docs/generate-api-table.js`. Founder may want the generator to also be runnable via `git pre-commit` hook (Sub-T4.d follow-up).

3. **Should the generator emit a "generated by" header comment in the sentinel region?** Recommend yes: `<!-- BEGIN: route-manifest-table — DO NOT EDIT; regenerate via 'node docs/generate-api-table.js' -->`. The README sentinel-research proposal does not specify this; T4 owner should align both files.

4. **`/api/exec` is deliberately a 501-returning stub** (manifest line 173 — "Deliberately deferred"). Should it appear in the generated table? Recommend yes (the manifest is the truth; the table mirrors the manifest verbatim; hiding `/api/exec` requires generator special-casing, which the generator should refuse). The description column will carry "Deliberately deferred — returns 501" so the reader is not misled.

5. **Wave numbering in the section heading** (`## API surface (post-Wave-3B + 3C)`, line 102) is hand-curated meta — not part of the table — and accumulates over time. Leave it alone for T4 (it's outside the sentinel region per §8a). Founder may want to renumber after the analysis-tools-unblock pass, but that's a hand-edit task, not a generator task.

6. **Does the meta-dogfood report's "API endpoint not found" finding for `/api/state` and `/api/manifests` mean those endpoints should be ADDED rather than the table fixed?** Reading the table prose (line 102 "post-Wave-3B + 3C") suggests these were aspirational ("we'll add `/api/state` and `/api/manifests` in Wave 3B"). The actual sprint never added them — `route-manifest.js` is the post-sprint truth. T4 deletes the fiction; if anyone wants those endpoints, they file separate tickets to add them to `app.js` + `route-manifest.js`. **T4 takes the manifest as authoritative.**

7. **Will T4's drift test conflict with the existing `route-manifest-drift.test.js`?** No — different concerns. The existing test enforces manifest ↔ `app.js` (implementation). The new T4 test enforces manifest ↔ docs. Both layers can fail independently; both are needed for a closed loop.

8. **The 23-wave sprint protocol section (lines 120–128) describes commits-per-ticket discipline** — when T4 commits, it should produce **at least 2 commits** by that discipline: (a) generator + initial sentinel insertion; (b) drift test. Possibly a third for `package.json` script. Founder protocol allows splitting; recommend one cohesive commit if the diff is small (manifest is frozen, generator is <50 LOC, test is <100 LOC).
