Let me check the directory and recall the key architecture decisions before writing the plan.
Good — all architecture decisions are consistent. Let me also get the exact current state of the files I need to modify, so the plan has precise before/after specs:
Now let me read the full index.js to understand the complete integration points:
Now I'll write the execution plan. This will be a comprehensive, agent-executable document. Given the scope, I'll create it in two files — a master plan and a detailed code specification.
Now the detailed phase specs — this is the core document agents will execute from:
Now let me update the memories and verify the plan is complete:
The execution plan is complete. Here's what I've delivered:

---

## Two Documents Created

### [EXECUTION-PLAN.md](/home/bozertron/1_AT_A_TIME/st8/.planning/st8_identity_system/EXECUTION-PLAN.md)
The master plan — architecture summary, dependency graph, agent assignment strategy, file inventory, and verification checklist.

### [PHASE-SPECS.md](/home/bozertron/1_AT_A_TIME/st8/.planning/st8_identity_system/PHASE-SPECS.md)
The detailed specification — every line of code, every SQL statement, every integration point. 1,927 lines of agent-executable instructions.

---

## What's in the Plan

| Phase | What It Does | Key Deliverable |
|-------|-------------|-----------------|
| **0** | `st8-types.js` — canonical types | `St8FileEntry`, `St8SchemaCard`, `St8MutationRecord`, `LifecyclePhase`, `MutationType`, `validateSt8FileEntry()`, `generateFingerprint()` |
| **1** | Schema overhaul — camelCase columns + `file_mutation_log` table | New `ST8_SCHEMA` for persistence.js + indexer.js, 7 new/updated methods in persistence.js |
| **2** | New modules — emitter, printer, notification bus | `SchemaCardEmitter` (JSON), `SchemaCardPrinter` (.txt fallback), `NotificationBus` (SSE + events + console) |
| **3** | Wire it all together | index.js mutation hooks, server.js SSE endpoint, _handleVerify fixes |
| **4** | Normalize the entire codebase | Every snake_case → camelCase, complete renaming map, migration script |
| **5** | Bootstrap on st8 itself | Fresh index, verify cards/mutations, test concept registration, MVP lock, PRD generation |
| **6** | Advanced features | `POST /api/concept-file`, `POST /api/mvp-lock`, `GET /api/prd`, `POST /api/production-promote`, SSE frontend integration |

## Agent Deployment Strategy

The plan is designed for 4 agents in parallel:
- **Agent A** → Phase 0 + Phase 1 (types + schema, tightly coupled)
- **Agent B** → Phase 2 (new modules, independent)
- **Agent C** → Phase 3 + Phase 4 (wiring + normalization, same files)
- **Agent D** → Phase 5 + Phase 6 (bootstrap + advanced, needs everything else first)

## The "First Hook" Trigger

Once Phases 0-4 are complete, running this single command triggers the full lifecycle:

```bash
cd /home/bozertron/1_AT_A_TIME/st8
rm -f st8.sqlite
node backend/index.js . --watch --serve --port 3847
```

This will index every file, emit schema cards, print .txt fallbacks, start watching, and open the SSE stream — the entire identity system comes alive on st8 itself.
