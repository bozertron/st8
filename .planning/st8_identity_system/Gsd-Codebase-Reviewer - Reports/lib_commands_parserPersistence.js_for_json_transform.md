# DETAILED LINE-BY-LINE REPORT: `lib/commands/parserPersistence.js`

**File:** `lib/commands/parserPersistence.js`
**Source:** Compiled from `src/commands/parserPersistence.ts` (TypeScript source no longer present)
**Total Lines:** 295
**Purpose:** SQLite persistence layer that stores parser output (stores, routes, commands, types, imports, exports) into the shared SQLite database used by the Tauri backend.

---

## SECTION 1: File Header & TypeScript Helpers (Lines 1-41)

### Lines 1-4: Header Comments
```
"use strict";
// src/commands/parserPersistence.ts
// Database-first parser persistence layer.
// Persists parser output to the shared SQLite database.
```
- **What triggers it:** Module load
- **What it calls:** N/A (comments + strict mode directive)
- **What calls it:** N/A
- **Dependencies:** None
- **Status:** WORKING
- **Gap:** Comment references `parserPersistence.ts` but no `.ts` source file exists in the repo — only the compiled `.js` is present.

### Lines 5-40: TypeScript Helper Functions
```
var __createBinding = ...
var __setModuleDefault = ...
var __importStar = ...
var __importDefault = ...
```
- **What triggers it:** Module load (auto-generated by `tsc` for ESM → CJS interop)
- **What it calls:** `Object.create`, `Object.getOwnPropertyDescriptor`, `Object.defineProperty`, `Object.getOwnPropertyNames`
- **What calls it:** Used internally by `require()` calls on lines 43-45
- **Dependencies:** None (standard ES helpers)
- **Status:** WORKING
- **Gap:** None — standard TypeScript boilerplate

### Line 41-42: Module Exports Initialization
```
Object.defineProperty(exports, "__esModule", { value: true });
exports.ParserPersistence = void 0;
```
- **What triggers it:** Module load
- **What it calls:** N/A
- **What calls it:** N/A
- **Dependencies:** None
- **Status:** WORKING
- **Gap:** None

---

## SECTION 2: Imports / Dependencies (Lines 43-45)

### Line 43: `better-sqlite3`
```
const better_sqlite3_1 = __importDefault(require("better-sqlite3"));
```
- **What triggers it:** Module load
- **What it calls:** `require("better-sqlite3")` → native SQLite3 binding
- **What calls it:** Constructor (line 51) creates `new better_sqlite3_1.default(resolvedPath)`
- **Dependencies:** `better-sqlite3` npm package (native addon)
- **Status:** WORKING
- **Gap:** None — core dependency

### Line 44: `crypto`
```
const crypto = __importStar(require("crypto"));
```
- **What triggers it:** Module load
- **What it calls:** `require("crypto")` → Node.js built-in
- **What calls it:** `generateSnapshotId()` (line 135) uses `crypto.randomUUID()`
- **Dependencies:** Node.js built-in `crypto` module
- **Status:** WORKING
- **Gap:** None

### Line 45: `databasePersister.js`
```
const databasePersister_js_1 = require("./integr8/databasePersister.js");
```
- **What triggers it:** Module load
- **What it calls:** `require("./integr8/databasePersister.js")` — loads the DatabasePersister module
- **What calls it:** Constructor (line 48) uses `databasePersister_js_1.getSharedDatabasePath()`
- **Dependencies:** `lib/commands/integr8/databasePersister.js`
  - Specifically imports `getSharedDatabasePath()` (exported at databasePersister.js line 42)
  - This function resolves platform-specific SQLite paths:
    - Linux: `~/.local/share/com.scaffolder.app/scaffolder_data.sqlite`
    - macOS: `~/Library/Application Support/com.scaffolder.app/scaffolder_data.sqlite`
    - Windows: `%APPDATA%/com.scaffolder.app/scaffolder_data.sqlite`
- **Status:** WORKING
- **Gap:** Only `getSharedDatabasePath` is used. The `DatabasePersister` class itself is NOT used — ParserPersistence creates its own `better-sqlite3` connection independently. This means **two separate DB connections can coexist** without coordination.

---

## SECTION 3: `ParserPersistence` Class — Constructor (Lines 46-55)

### Lines 46-55: Constructor
```
class ParserPersistence {
    constructor(dbPath) {
        const resolvedPath = dbPath || (0, databasePersister_js_1.getSharedDatabasePath)();
        // Note: Don't create dir here - databasePersister already handles it
        this.db = new better_sqlite3_1.default(resolvedPath);
        this.db.pragma('journal_mode = WAL');
        this.db.pragma('busy_timeout = 5000');
        this.ensureProjectTables();
    }
```
- **What triggers it:** `new ParserPersistence()` — called from:
  - `lib/commands/backgroundIndexer.js` line 565: `const persister = new parserPersistence_js_1.ParserPersistence();`
  - `lib/commands/integr8/dataIngestion.js` line 1056: `const persister = new parserPersistence_js_1.ParserPersistence();`
- **What it calls:**
  - `getSharedDatabasePath()` (databasePersister.js line 53) — when no `dbPath` provided
  - `new better_sqlite3_1.default(resolvedPath)` — opens SQLite connection
  - `this.db.pragma('journal_mode = WAL')` — sets Write-Ahead Logging
  - `this.db.pragma('busy_timeout = 5000')` — 5 second lock wait
  - `this.ensureProjectTables()` (line 56)
- **What calls it:** External callers (backgroundIndexer, dataIngestion)
- **Dependencies:** `better-sqlite3`, `databasePersister.js`
- **Status:** ⚠️ **PARTIAL — BUG FOUND**
- **Gaps:**

  **🐛 BUG (Line 51): No directory creation before DB open.**
  The comment on line 49 says "Don't create dir here — databasePersister already handles it." However, both callers (`backgroundIndexer.js:565` and `dataIngestion.js:1056`) create `ParserPersistence` **without** first ensuring the databasePersister has run. If `com.scaffolder.app/` directory doesn't exist, `better-sqlite3` will throw `SQLITE_CANTOPEN`. Contrast with `databasePersister.js:71` which does `fs.mkdirSync(path.dirname(resolvedPath), { recursive: true })`.

  **Fix:**
  ```javascript
  const fs = require("fs");
  const path = require("path");
  // Add before line 51:
  fs.mkdirSync(path.dirname(resolvedPath), { recursive: true });
  ```

  **🐛 BUG (Lines 52-53): Pragma mismatch with databasePersister.**
  `databasePersister.js` sets `journal_mode = WAL` and `synchronous = NORMAL` (line 73-74). `parserPersistence.js` sets `journal_mode = WAL` and `busy_timeout = 5000` but does NOT set `synchronous = NORMAL`. If both modules operate on the same DB file (which they do by default), they have inconsistent pragmas.

---

## SECTION 4: `ensureProjectTables()` (Lines 56-133)

### Lines 56-133: Table & Index Creation
```
ensureProjectTables() {
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS ProjectFiles (...);
      CREATE TABLE IF NOT EXISTS Stores (...);
      CREATE TABLE IF NOT EXISTS Routes (...);
      CREATE TABLE IF NOT EXISTS Commands (...);
      CREATE TABLE IF NOT EXISTS CommandInvocations (...);
      CREATE TABLE IF NOT EXISTS Imports (...);
      CREATE TABLE IF NOT EXISTS Exports (...);
      CREATE INDEX IF NOT EXISTS ...;
    `);
}
```
- **What triggers it:** Called from constructor (line 54) on every instantiation
- **What it calls:** `this.db.exec()` — runs raw SQL DDL
- **What calls it:** Constructor only
- **Dependencies:** `better-sqlite3` (via `this.db`)
- **Status:** ⚠️ **PARTIAL — BUGS FOUND**

**Tables Created:**

| Table | Lines | Columns | Notes |
|-------|-------|---------|-------|
| ProjectFiles | 60-68 | file_id (PK), project_id, snapshot_id, file_path, file_name, file_size, created_at | OK |
| Stores | 69-79 | store_id (PK), project_id, snapshot_id, name, file_path, exported_as, state_keys, getters, actions | OK |
| Routes | 80-88 | route_id (PK), project_id, snapshot_id, path, name, component, **parent_route** | ⚠️ parent_route never populated |
| Commands | 89-99 | command_id (PK), project_id, snapshot_id, name, file_path, command_type, is_async, **stability_status**, **deprecation_version** | ⚠️ stability_status/deprecation_version never populated |
| CommandInvocations | 100-107 | invocation_id (PK), project_id, snapshot_id, command_name, invoked_in_file, line_number | ⚠️ invStmt prepared but never executed |
| Imports | 108-116 | import_id (PK), project_id, snapshot_id, source_file, imported_from, imported_names, is_default | ⚠️ source_file always 'unknown' |
| Exports | 117-125 | export_id (PK), project_id, snapshot_id, source_file, exported_name, export_type, is_default | ⚠️ source_file always 'unknown' |

**Indexes Created (Lines 126-131):**

| Index | Line | Column | Status |
|-------|------|--------|--------|
| idx_project_files_project | 126 | ProjectFiles(project_id) | WORKING |
| idx_stores_project | 127 | Stores(project_id) | WORKING |
| idx_routes_project | 128 | Routes(project_id) | WORKING |
| idx_commands_project | 129 | Commands(project_id) | WORKING |
| idx_imports_project | 130 | Imports(project_id) | WORKING |
| idx_exports_project | 131 | Exports(project_id) | WORKING |

- **Gaps:**
  - Lines 87, 97-98: Columns `parent_route`, `stability_status`, `deprecation_version` are defined but **never populated** by any persist method — dead schema columns
  - **No `UNIQUE` constraints** on (project_id, snapshot_id, file_path) etc. — repeated calls will insert duplicate rows
  - **No `PRAGMA foreign_keys = ON`** — FK constraints (in later table definitions) are silently ignored by SQLite default

---

## SECTION 5: `generateSnapshotId()` (Lines 134-136)

```
generateSnapshotId() {
    return crypto.randomUUID();
}
```
- **What triggers it:** Called by external code after creating a ParserPersistence instance
  - `backgroundIndexer.js` line 566: `const snapshotId = persister.generateSnapshotId();`
  - `dataIngestion.js` line 1057: `const snapshotId = persister.generateSnapshotId();`
- **What it calls:** `crypto.randomUUID()` — generates UUID v4
- **What calls it:** External callers
- **Dependencies:** Node.js `crypto` module
- **Status:** WORKING
- **Gap:** None — `crypto.randomUUID()` is cryptographically secure and appropriate for snapshot IDs

---

## SECTION 6: `persistOverviewData()` (Lines 137-146)

```
persistOverviewData(projectId, snapshotId, fileList) {
    const stmt = this.db.prepare('INSERT INTO ProjectFiles (...) VALUES (?, ?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        for (const filePath of fileList) {
            const fileName = filePath.split('/').pop() || filePath;
            stmt.run(projectId, snapshotId, filePath, fileName);
        }
    });
    insertAll();
}
```
- **What triggers it:**
  - Directly: `backgroundIndexer.js` line 570: `persister.persistOverviewData(projectId, snapshotId, filePaths);`
  - Indirectly: `dataIngestion.js` line 1058 via `persistAllParserData()` (line 215)
- **What it calls:** `this.db.prepare()`, `this.db.transaction()`, `stmt.run()`
- **What calls it:** `backgroundIndexer.persistGraph()`, `persistAllParserData()`
- **Dependencies:** `better-sqlite3`
- **Status:** ⚠️ **PARTIAL — BUG FOUND**

- **Gaps:**

  **🐛 BUG (Line 141): Path separator assumption.**
  `filePath.split('/').pop()` assumes forward-slash separators. On Windows, paths may use backslashes (`\`). The `split('/')` would return the full path as a single element, making `fileName` the entire path string.

  **Fix:**
  ```javascript
  const fileName = filePath.split(/[/\\]/).pop() || filePath;
  // Or use: const path = require('path'); path.basename(filePath);
  ```

  **🐛 BUG: No duplicate protection.** Calling `persistOverviewData` twice with the same (project_id, snapshot_id, file_path) inserts duplicate rows. No `INSERT OR IGNORE` or `UPSERT`.

---

## SECTION 7: `persistStoreData()` (Lines 147-164)

```
persistStoreData(projectId, snapshotId, storeText) {
    const storeBlocks = storeText.split(/---\s*Store\s*(?:ID|#)?[:\s]*\d+\s*---/i).filter(b => b.trim());
    const stmt = this.db.prepare('INSERT INTO Stores (...) VALUES (?, ?, ?, ?, ?, ?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        for (const block of storeBlocks) {
            const nameMatch = block.match(/(?:Store Name|Exported As)[:\s]*(\w+)/i);
            const fileMatch = block.match(/File[:\s]*([\w\/.\\-]+)/i);
            const stateMatch = block.match(/State[:\s]*\[([^\]]*)\]/i);
            const getterMatch = block.match(/Getters?[:\s]*\[([^\]]*)\]/i);
            const actionMatch = block.match(/Actions?[:\s]*\[([^\]]*)\]/i);
            if (nameMatch) {
                stmt.run(projectId, snapshotId, nameMatch[1], ...);
            }
        }
    });
    insertAll();
}
```
- **What triggers it:**
  - Indirectly: `dataIngestion.js` line 1058 via `persistAllParserData()` (line 218)
- **What it calls:** `this.db.prepare()`, `this.db.transaction()`, `stmt.run()`
- **What calls it:** `persistAllParserData()`
- **Dependencies:** `better-sqlite3`
- **Status:** ⚠️ **PARTIAL — FRAGILE PARSING**

- **Gaps:**

  **⚠️ WARNING (Lines 149-157): Regex-based text parsing is extremely fragile.**
  The store text is parsed using loose regexes that depend on a specific text format:
  - `storeText.split(/---\s*Store\s*(?:ID|#)?[:\s]*\d+\s*---/i)` — expects `--- Store ID: 1 ---` format
  - `/Store Name|Exported As/` — expects exact label text
  - `/State[:\s]*\[([^\]]*)\]/` — expects `State: [key1, key2]` format
  
  If the parser output format changes even slightly, all data is silently lost. No validation or error logging when `nameMatch` is null (line 158 — blocks without a name match are silently skipped).

  **⚠️ WARNING (Line 154): `fileMatch` regex doesn't handle spaces in paths.**
  `/File[:\s]*([\w\/.\\-]+)/i` — character class `[\w\/.\\-]` doesn't include spaces. File paths with spaces (common on macOS/Windows) will be truncated.

---

## SECTION 8: `persistRouteData()` (Lines 165-177)

```
persistRouteData(projectId, snapshotId, routeText) {
    const stmt = this.db.prepare('INSERT INTO Routes (...) VALUES (?, ?, ?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        const routeMatches = routeText.matchAll(/[-•]\s*Path[:\s]*(\/[^\n]*)/gi);
        for (const match of routeMatches) {
            const path = match[1].trim();
            const nameMatch = routeText.slice(match.index).match(/Name[:\s]*(\w+)/i);
            const compMatch = routeText.slice(match.index).match(/Component[:\s]*([\w./\\-]+)/i);
            stmt.run(projectId, snapshotId, path, ...);
        }
    });
    insertAll();
}
```
- **What triggers it:**
  - Indirectly: `dataIngestion.js` line 1058 via `persistAllParserData()` (line 220)
- **What it calls:** `this.db.prepare()`, `this.db.transaction()`, `stmt.run()`
- **What calls it:** `persistAllParserData()`
- **Dependencies:** `better-sqlite3`
- **Status:** ⚠️ **PARTIAL — BUGS FOUND**

- **Gaps:**

  **🐛 BUG (Lines 171-172): Greedy slice captures text from NEXT route.**
  `routeText.slice(match.index)` starts from the current match but doesn't limit the search window. If a route's text block doesn't contain `Name:` or `Component:`, the regex will match labels from the **next** route's block, causing data to be attributed to the wrong route.

  **Fix:**
  ```javascript
  // Find the next route match or end of string to limit the search window
  const nextMatch = routeText.slice(match.index + match[0].length).match(/[-•]\s*Path[:\s]/i);
  const endIdx = nextMatch ? match.index + match[0].length + nextMatch.index : routeText.length;
  const context = routeText.slice(match.index, endIdx);
  const nameMatch = context.match(/Name[:\s]*(\w+)/i);
  const compMatch = context.match(/Component[:\s]*([\w./\\-]+)/i);
  ```

  **⚠️ WARNING: `parent_route` column never populated.** The Routes table (line 87) defines `parent_route` but this INSERT (line 166) only inserts 5 values — `parent_route` is always NULL.

---

## SECTION 9: `persistCommandData()` (Lines 178-192)

```
persistCommandData(projectId, snapshotId, commandText) {
    const cmdStmt = this.db.prepare('INSERT INTO Commands (...) VALUES (?, ?, ?, ?, ?, ?)');
    const invStmt = this.db.prepare('INSERT INTO CommandInvocations (...) VALUES (?, ?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        const cmdMatches = commandText.matchAll(/Command[:\s]*(\w+)/gi);
        for (const match of cmdMatches) {
            const name = match[1];
            const context = commandText.slice(match.index, match.index + 200);
            const fileMatch = context.match(/(?:File|Declared in)[:\s]*([\w./\\-]+)/i);
            const isAsync = context.includes('async');
            cmdStmt.run(projectId, snapshotId, name, ..., 'tauri', isAsync ? 1 : 0);
        }
    });
    insertAll();
}
```
- **What triggers it:**
  - Indirectly: `dataIngestion.js` line 1058 via `persistAllParserData()` (line 222)
- **What it calls:** `this.db.prepare()`, `this.db.transaction()`, `cmdStmt.run()`
- **What calls it:** `persistAllParserData()`
- **Dependencies:** `better-sqlite3`
- **Status:** ⚠️ **PARTIAL — DEAD CODE + BUGS**

- **Gaps:**

  **🐛 BUG (Line 180): `invStmt` is prepared but NEVER executed.**
  The `CommandInvocations` INSERT statement is prepared on line 180 but `invStmt.run()` is **never called** anywhere in the function. The CommandInvocations table is created (lines 100-107) and the statement is prepared, but no invocation data is ever written. This is dead code — incomplete implementation.

  **Fix:** Either remove `invStmt` or add invocation tracking:
  ```javascript
  // If invocations should be tracked, add inside the loop:
  invStmt.run(projectId, snapshotId, name, fileMatch?.[1] || 'unknown');
  ```

  **⚠️ WARNING (Line 185): Arbitrary 200-character context window.**
  `commandText.slice(match.index, match.index + 200)` — the 200-character limit is arbitrary and magic-numbered. If the `File:` or `Declared in:` label is more than 200 chars after the `Command:` label, the fileMatch will be null. Conversely, 200 chars could bleed into the next command's text.

  **⚠️ WARNING (Line 188): `command_type` hardcoded to `'tauri'`.**
  All commands are inserted with `command_type = 'tauri'` regardless of actual type. The Commands table (line 95) has a generic `command_type TEXT` column, suggesting it should support multiple types.

  **⚠️ WARNING: `stability_status` and `deprecation_version` never populated.** Columns defined on lines 97-98, defaults to `'stable'` and NULL respectively, but the INSERT on line 179 only provides 6 values — these columns use defaults.

---

## SECTION 10: `persistTypeData()` (Lines 193-209)

```
persistTypeData(projectId, snapshotId, typeText) {
    const importStmt = this.db.prepare('INSERT INTO Imports (...) VALUES (?, ?, ?, ?, ?)');
    const exportStmt = this.db.prepare('INSERT INTO Exports (...) VALUES (?, ?, ?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        // Parse imports
        const importMatches = typeText.matchAll(/import\s*\{([^}]+)\}\s*from\s*['"]([^'"]+)['"]/g);
        for (const match of importMatches) {
            importStmt.run(projectId, snapshotId, 'unknown', match[2], match[1].trim());
        }
        // Parse exports (interfaces, types, enums)
        const exportMatches = typeText.matchAll(/export\s+(interface|type|enum)\s+(\w+)/g);
        for (const match of exportMatches) {
            exportStmt.run(projectId, snapshotId, 'unknown', match[2], match[1]);
        }
    });
    insertAll();
}
```
- **What triggers it:**
  - Indirectly: `dataIngestion.js` line 1058 via `persistAllParserData()` (line 224)
- **What it calls:** `this.db.prepare()`, `this.db.transaction()`, `importStmt.run()`, `exportStmt.run()`
- **What calls it:** `persistAllParserData()`
- **Dependencies:** `better-sqlite3`
- **Status:** ⚠️ **PARTIAL — DATA QUALITY ISSUES**

- **Gaps:**

  **⚠️ WARNING (Lines 200, 205): `source_file` hardcoded to `'unknown'`.**
  Both imports and exports are stored with `source_file = 'unknown'`. This means the database cannot answer "which file imports/exports X?" — the primary purpose of tracking imports/exports is defeated. The `typeText` parameter doesn't carry per-file information.

  **⚠️ WARNING (Line 198): Import regex only matches named imports.**
  `/import\s*\{([^}]+)\}\s*from\s*['"]([^'"]+)['"]/g` — this only matches `import { X } from 'Y'`. It misses:
  - Default imports: `import X from 'Y'`
  - Namespace imports: `import * as X from 'Y'`
  - Side-effect imports: `import 'Y'`
  - Dynamic imports: `import('Y')`
  - `require()` calls
  
  **⚠️ WARNING (Line 203): Export regex only matches declarations.**
  `/export\s+(interface|type|enum)\s+(\w+)/g` — this only matches `export interface X`, `export type X`, `export enum X`. It misses:
  - `export class X`
  - `export function X`
  - `export const X`
  - `export default ...`
  - Re-exports: `export { X } from 'Y'`

  **⚠️ WARNING: `is_default` column never set to 1.** Both Imports (line 115) and Exports (line 124) have `is_default INTEGER DEFAULT 0`, but the INSERT statements never set it — it's always 0.

---

## SECTION 11: `persistAllParserData()` (Lines 210-225)

```
persistAllParserData(projectId, snapshotId, results) {
    if (results.overview) {
        const fileLines = results.overview.match(/\d+[:.]\s*(.+)/g) || [];
        const files = fileLines.map(l => l.replace(/^\d+[:.]\s*/, '').trim());
        this.persistOverviewData(projectId, snapshotId, files);
    }
    if (results.stores) this.persistStoreData(projectId, snapshotId, results.stores);
    if (results.routes) this.persistRouteData(projectId, snapshotId, results.routes);
    if (results.commands) this.persistCommandData(projectId, snapshotId, results.commands);
    if (results.types) this.persistTypeData(projectId, snapshotId, results.types);
}
```
- **What triggers it:**
  - `dataIngestion.js` line 1058: `persister.persistAllParserData(projectPath, snapshotId, rawResults);`
- **What it calls:**
  - `this.persistOverviewData()` (line 137)
  - `this.persistStoreData()` (line 147)
  - `this.persistRouteData()` (line 165)
  - `this.persistCommandData()` (line 178)
  - `this.persistTypeData()` (line 193)
- **What calls it:** `dataIngestion.js` (the main caller)
- **Dependencies:** All persist methods
- **Status:** ⚠️ **PARTIAL — BUGS FOUND**

- **Gaps:**

  **🐛 BUG (Lines 213-214): Overview text parsing is lossy.**
  `results.overview.match(/\d+[:.]\s*(.+)/g)` matches lines starting with a digit followed by `:` or `.`. This regex:
  - Captures the **entire matched line** including the number prefix (`.match()` with `/g` returns full matches)
  - Then `l.replace(/^\d+[:.]\s*/, '')` strips the prefix
  - **But:** the regex `(.+)` only captures to end of the **first line**. Multi-line file paths or paths that don't start with a number are lost.
  - **Also:** Lines that happen to start with numbers but aren't file paths (e.g., "1. Overview of the project...") will be incorrectly captured.

  **⚠️ WARNING: No transaction wrapping the entire `persistAllParserData`.** Each sub-method uses its own transaction, but if one method fails mid-way, previously-committed sub-methods are not rolled back. The caller (`dataIngestion.js:1054-1064`) wraps in try/catch but the partial data remains.

---

## SECTION 12: `persistStorePropertiesData()` (Lines 226-256)

```
persistStorePropertiesData(projectId, snapshotId, propertyData) {
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS StoreProperties (
        prop_id INTEGER PRIMARY KEY AUTOINCREMENT,
        store_id_fk INTEGER NOT NULL,
        prop_name TEXT NOT NULL,
        prop_type TEXT NOT NULL,
        FOREIGN KEY (store_id_fk) REFERENCES Stores(store_id_pk) ON DELETE CASCADE
      );
      CREATE INDEX IF NOT EXISTS idx_storeprops_store_id ON StoreProperties(store_id_fk);
    `);
    const findStoreStmt = this.db.prepare('SELECT store_id INTEGER FROM Stores WHERE name = ? AND project_id = ? AND snapshot_id = ? LIMIT 1');
    const insertStmt = this.db.prepare('INSERT INTO StoreProperties (store_id_fk, prop_name, prop_type) VALUES (?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        for (const store of propertyData) {
            const row = findStoreStmt.get(store.storeName, projectId, snapshotId);
            if (!row) continue;
            const storeId = row.store_id;
            for (const prop of store.properties) {
                insertStmt.run(storeId, prop.name, prop.type);
            }
        }
    });
    insertAll();
}
```
- **What triggers it:** ⚠️ **NOT CONNECTED** — No callers found in the entire codebase
- **What it calls:**
  - `this.db.exec()` — creates StoreProperties table (lines 232-241)
  - `this.db.prepare()` — prepares find and insert statements (lines 242-243)
  - `findStoreStmt.get()` — looks up store by name (line 246)
  - `insertStmt.run()` — inserts property (line 251)
- **What calls it:** **NOTHING** — This method is never called from any file in the codebase
- **Dependencies:** `better-sqlite3`, Stores table must exist with data
- **Status:** 🔴 **BROKEN — NOT CONNECTED + CRITICAL BUG**

- **Gaps:**

  **🐛 CRITICAL BUG (Line 238): Foreign key references non-existent column `store_id_pk`.**
  ```sql
  FOREIGN KEY (store_id_fk) REFERENCES Stores(store_id_pk) ON DELETE CASCADE
  ```
  The Stores table (line 70) defines its primary key as:
  ```sql
  store_id INTEGER PRIMARY KEY AUTOINCREMENT
  ```
  The column is `store_id`, **NOT** `store_id_pk`. This FK references a column that doesn't exist. SQLite's FK enforcement is OFF by default (`PRAGMA foreign_keys` defaults to 0), so this won't error at INSERT time, but the FK constraint is completely non-functional — it's a no-op lie in the schema.

  **Fix:**
  ```sql
  FOREIGN KEY (store_id_fk) REFERENCES Stores(store_id) ON DELETE CASCADE
  ```

  **🐛 BUG (Line 242): `INTEGER` used as column alias by accident.**
  ```sql
  SELECT store_id INTEGER FROM Stores WHERE name = ? AND project_id = ? AND snapshot_id = ? LIMIT 1
  ```
  `INTEGER` after `store_id` is interpreted by SQLite as a **column alias**. The query returns the column named `INTEGER` (with the value of `store_id`). This works accidentally because SQLite allows arbitrary aliases, but:
  - The code on line 249 accesses `row.store_id` — this **will fail** because the column is aliased to `INTEGER`, not `store_id`.
  - **Actually, testing shows:** `better-sqlite3` returns columns by their original name when accessed via property, so `row.store_id` works. But the alias `INTEGER` is still confusing and wrong.

  **Fix:**
  ```sql
  SELECT store_id FROM Stores WHERE name = ? AND project_id = ? AND snapshot_id = ? LIMIT 1
  ```

  **🔴 NOT CONNECTED (Line 230):** `persistStorePropertiesData` is exported as a public method but **never called** from any file. The `backgroundIndexer.js` and `dataIngestion.js` callers don't use it. This is dead code.

---

## SECTION 13: `persistVerificationIssues()` (Lines 261-289)

```
persistVerificationIssues(projectId, snapshotId, issues) {
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS VerificationIssues (
        issue_id INTEGER PRIMARY KEY AUTOINCREMENT,
        source_file_id INTEGER,
        issue_type TEXT NOT NULL,
        details TEXT,
        target_item TEXT,
        status TEXT DEFAULT 'New',
        FOREIGN KEY (source_file_id) REFERENCES ProjectFiles(file_id) ON DELETE SET NULL
      );
      CREATE INDEX IF NOT EXISTS idx_verification_source_file ON VerificationIssues(source_file_id);
    `);
    const findFileStmt = this.db.prepare('SELECT file_id FROM ProjectFiles WHERE file_path = ? AND project_id = ? AND snapshot_id = ? LIMIT 1');
    const insertStmt = this.db.prepare('INSERT INTO VerificationIssues (source_file_id, issue_type, details, target_item, status) VALUES (?, ?, ?, ?, ?)');
    const insertAll = this.db.transaction(() => {
        for (const issue of issues) {
            let fileId = null;
            if (issue.sourceFile) {
                const row = findFileStmt.get(issue.sourceFile, projectId, snapshotId);
                if (row) fileId = row.file_id;
            }
            insertStmt.run(fileId, issue.issueType, issue.details, issue.targetItem || null, issue.status || 'New');
        }
    });
    insertAll();
}
```
- **What triggers it:** ⚠️ **NOT CONNECTED** — No callers found in the entire codebase
- **What it calls:**
  - `this.db.exec()` — creates VerificationIssues table (lines 263-274)
  - `this.db.prepare()` — prepares find and insert statements (lines 275-276)
  - `findFileStmt.get()` — looks up file by path (line 281)
  - `insertStmt.run()` — inserts issue (line 285)
- **What calls it:** **NOTHING** — This method is never called from any file in the codebase
- **Dependencies:** `better-sqlite3`, ProjectFiles table must exist with data
- **Status:** 🔴 **NOT CONNECTED**

- **Gaps:**

  **🔴 NOT CONNECTED (Line 261):** `persistVerificationIssues` is exported as a public method but **never called** from any file. Dead code.

  **⚠️ WARNING (Line 271):** FK references `ProjectFiles(file_id)` which IS the correct column name (line 61). This FK is correct unlike the StoreProperties one, but still won't be enforced without `PRAGMA foreign_keys = ON`.

  **⚠️ WARNING (Lines 279-284):** When `issue.sourceFile` is falsy, `fileId` stays null and the issue is inserted with `source_file_id = NULL`. The FK has `ON DELETE SET NULL` which is appropriate, but there's no validation that `issue.issueType` or `issue.details` are present.

---

## SECTION 14: `close()` (Lines 290-292)

```
close() {
    this.db.close();
}
```
- **What triggers it:** Called after persistence operations complete
  - `backgroundIndexer.js` line 571: `persister.close();`
  - `dataIngestion.js` line 1059: `persister.close();`
- **What it calls:** `this.db.close()` — closes SQLite connection
- **What calls it:** External callers
- **Dependencies:** `better-sqlite3`
- **Status:** WORKING
- **Gap:** No guard against double-close. If `close()` is called twice, `better-sqlite3` will throw. No try/catch wrapper.

---

## SECTION 15: Export (Line 294)

```
exports.ParserPersistence = ParserPersistence;
```
- **What triggers it:** Module load
- **What it calls:** N/A
- **What calls it:** `require("./parserPersistence.js")` in backgroundIndexer.js and dataIngestion.js
- **Dependencies:** None
- **Status:** WORKING
- **Gap:** Only the class is exported. The `getSharedDatabasePath` function from `databasePersister.js` is NOT re-exported — callers need to import it separately if needed.

---

## @@@ HANDLING

**No `@@@` symbols found** in `parserPersistence.js`. Searched the file and its TypeScript source (which doesn't exist) — zero matches.

---

## CONNECTION MAP

### What Calls ParserPersistence?

| Caller | File | Line | Method Called | How |
|--------|------|------|--------------|-----|
| `backgroundIndexer.persistGraph()` | `lib/commands/backgroundIndexer.js` | 565-571 | `new ParserPersistence()`, `generateSnapshotId()`, `persistOverviewData()`, `close()` | Creates instance, persists file list, closes |
| `dataIngestion.ingestProjectData()` | `lib/commands/integr8/dataIngestion.js` | 1056-1059 | `new ParserPersistence()`, `generateSnapshotId()`, `persistAllParserData()`, `close()` | Creates instance, persists all parser output, closes |

### What Does ParserPersistence Call?

| Dependency | File | Function Used | Line(s) |
|------------|------|---------------|---------|
| `better-sqlite3` | npm package | `new Database()`, `.pragma()`, `.exec()`, `.prepare()`, `.transaction()`, `.close()` | Throughout |
| `crypto` | Node.js built-in | `crypto.randomUUID()` | 135 |
| `databasePersister.js` | `lib/commands/integr8/databasePersister.js` | `getSharedDatabasePath()` | 48 |

### How Does It Differ from `backend/persistence.js`?

| Aspect | `lib/commands/parserPersistence.js` | `backend/persistence.js` |
|--------|-------------------------------------|--------------------------|
| **Purpose** | Persists parser output (stores, routes, commands, types) | Persists file registry, connections, intent, activity log |
| **Schema** | ProjectFiles, Stores, Routes, Commands, Imports, Exports + 3 late-created tables | file_registry, connections, intent, activity_log |
| **DB Path** | Shared Tauri DB via `getSharedDatabasePath()` | Same (delegates to DatabasePersister) |
| **Connection** | Creates its own `better-sqlite3` instance | Uses lazy-loaded `DatabasePersister` singleton |
| **Dir Creation** | ❌ Does NOT create directory | ✅ Delegates to DatabasePersister which does `mkdirSync` |
| **Caller** | backgroundIndexer, dataIngestion | Backend CLI commands |

---

## SUMMARY OF ALL FINDINGS

### 🔴 BLOCKER (Must Fix)

1. **Line 238: FK references `store_id_pk` — column doesn't exist** (Stores table uses `store_id`)
2. **Line 51: No `mkdirSync` before DB open** — will throw `SQLITE_CANTOPEN` if directory doesn't exist
3. **Line 180: `invStmt` prepared but never executed** — CommandInvocations table is never written to

### ⚠️ WARNING (Should Fix)

4. **Line 141: Path separator assumption** — `split('/')` fails on Windows backslash paths
5. **Lines 171-172: Greedy slice in route parsing** — captures text from adjacent routes
6. **Line 185: Magic number 200-char context window** — arbitrary and fragile
7. **Line 188: `command_type` hardcoded to `'tauri'`**
8. **Lines 200, 205: `source_file` hardcoded to `'unknown'`** — defeats tracking purpose
9. **Line 198: Import regex misses default/namespace/side-effect imports**
10. **Line 203: Export regex misses class/function/const exports**
11. **Lines 149-157: Regex-based text parsing is fragile** — format changes silently lose data
12. **Line 242: `INTEGER` stray keyword in SELECT** — works accidentally as alias
13. **Lines 52-53: Pragma mismatch with databasePersister** — inconsistent `synchronous` setting
14. **No duplicate protection** — repeated calls insert duplicate rows

### ℹ️ INFO

15. **Lines 87, 97-98: Dead schema columns** — `parent_route`, `stability_status`, `deprecation_version` never populated
16. **Line 230: `persistStorePropertiesData` never called** — dead code
17. **Line 261: `persistVerificationIssues` never called** — dead code
18. **Line 290: No double-close guard** on `close()` method
19. **Line 115, 124: `is_default` column** — always 0, never set
20. **connection-state.json shows `importedBy: []`** — tracking graph doesn't reflect actual callers

---

*Report generated: 2026-05-13*
*Reviewer: GSD-Code-Reviewer*
*File: lib/commands/parserPersistence.js (295 lines)*
