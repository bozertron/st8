# st8 — Working Notes for Claude

Read this **first** in every session that touches this repo.

## The real refactored tree (canonical)

```
src/core/        # infrastructure
  ├── hook-registry.js        # HookRegistry, HOOKS constants
  ├── notification-bus.js     # SSE bus
  ├── database/               # persistence.js, graph-persister.js
  ├── hooks/                  # default-subscribers.js, force-checks.js
  └── server/                 # main.js (bootstrap), app.js (HTTP), auth.js

src/features/    # capability modules
  ├── analysis/               # gap-analyzer, intent-seeder, insight-store,
  │                             path-generator, signal-path-adapter,
  │                             report-generator, insight-store-populator
  ├── indexing/               # indexer, ast-related parsers, data-ingestion
  ├── schema-cards/           # emitter, printer, manifest-generator
  ├── graph/                  # builder, traversal
  ├── lifecycle/              # bruno-oscar
  ├── prd/                    # generator, template-engine
  ├── search/                 # sonic-client, sonic-indexer, sonic-daemon
  ├── watcher/                # file-watcher
  └── integr8/                # vendored migration toolkit (read-only)

src/shared/      # cross-cutting
  ├── types/                  # st8-types.js (generateFingerprint, MutationType, ...)
  └── utils/                  # ast-parser, birth-timestamp, safe-fs

src/frontend/    # UI
  └── app.js, components/, styles/, vendor/
```

## Entry-point chain

```
start.js (repo root, 148 LOC)        ← process entry: parses argv, installs deps, spawns backend
   │   spawn('node', ['src/core/server/main.js', targetDir, '--port', N, '--serve'])
   ▼
src/core/server/main.js (453 LOC)    ← backend bootstrap: wires indexer + persistence
   │                                    + manifest + watcher + St8Server + hookRegistry
   │   new St8Server(...).listen(...)
   ▼
src/core/server/app.js (2891 LOC)    ← HTTP API surface (St8Server class)
                                       All /api/* routes live here.
                                       This is where most edits land.
```

**`app.js` is the HTTP layer, NOT the entry point.** `start.js` → `main.js` → `app.js`.

## Tests

Runner: `node --test` (Node 18+, zero dependencies).
Layout: `tests/` mirrors `src/` (e.g. `tests/core/hook-registry.test.js`).
Current count: **538 passing, 1 pre-existing failing (`ogb-destroy.test.js` — master-side, unrelated), 0 skipped, 0 todo**.
Run: `npm test`.
Conventions: `tests/README.md`.

## Authoritative file inventory

`st8-filemap.md` — auto-generated by `docs/generate-filemap.js`. If you need a complete list of source files with line counts and dependencies, read that.

## HISTORICAL ARTIFACTS — DO NOT WORK ON

These exist in git history or as documentary references. They are **not** work targets:

- `src/0_core/`, `src/0_features/`, `src/0_shared/`, `src/0_frontend/` — planning skeleton (103 empty files) created in commit `36d9c16` and **deleted** in commit `d340af4` ("batch 021 — 0_* cleanup"). Currently zero files in the tree start with `0_`.
- Twelve root-level `0_*.md` planning docs (`0_MASTER_INDEX.md`, `0_BACKEND_INDEX.md`, etc.) — archived in the May 16 master cleanup commit. They described a refactor scheme that was abandoned.
- `OGB/` directory — retired code preserved for reference, never imported.
- `backend/`, `lib/` — the **old** pre-refactor layout. All code migrated to `src/` by Aria's migration batches (batches 001-020 in `st8_bible.md`). These directories no longer exist.

**If an external review tool reports "src/0_* is empty stubs" or cites commit 36d9c16 as "most recent" — it is reading from a stale clone. Ignore it. Trust `git log -1 --format=%H` and `ls src/`.**

## Hooks / signal flow

Canonical hook constants in `src/core/hook-registry.js`:
- `INDEX_START`, `INDEX_COMPLETE`
- `FILE_INDEXED`, `FILE_BEFORE_CHANGE`, `FILE_AFTER_CHANGE`
- `LIFECYCLE_TRANSITION`, `COMMIT_RECORDED`
- `PRD_GENERATE`, `TICKET_CREATED`

All have at least one publisher. Subscribers register in `src/core/hooks/default-subscribers.js` with explicit `source` tags and priorities (lower priority runs first). Per-subscriber convention: **DRY + wrap** — each subscriber wraps its own body in try/catch with `[st8] <source> failed: <msg>` log; registry-level catch is the safety net.

## Persistence invariants (post-Wave-1)

- `PRAGMA foreign_keys = ON` in `initialize()`.
- `EXPECTED_SCHEMA` + `introspectSchema()` boot-time drift detector.
- `getSharedPersistence()` singleton for HTTP handlers (~86× speedup vs per-request init).
- `providers` table mirrors LLM_PROVIDERS + 'human'; `claimTicket` validates against it.
- Per-fingerprint cascade in `deleteFile` and `pruneFilesNotIn` (multi-fingerprint-per-filepath safe).
- File-mutation-log retention: P=50 INDEX_COMPLETE subscriber, 24h gate via st8_settings.

## Identity invariants (post-Wave-3A)

- `birthTimestamp` reuse via `getFileByPath` — first observation wins across reindex passes.
- `isUnreliableBirthtime` detects Unix-epoch / pre-1980 stat.birthtime fallback to mtime.
- `.st8/identity-risk.json` written when reuse rescues an unreliable birthtime.
- Multi-fingerprint card dedup in `emitter.js` — newer birthTimestamp wins deterministically.

## API surface (post-Batch-032)

**Source of truth: `src/core/server/route-manifest.js`** — hand-edited, 1:1 with `app.js`'s switch enforced by `tests/core/server/route-manifest-drift.test.js`. The table below is a snapshot synced from that manifest. Any new route MUST land in BOTH `app.js` and `route-manifest.js` or the drift test fails.

| Route | Method | Purpose | Auth |
|---|---|---|---|
| `/api/connection-state.json` | GET | Live manifest JSON (in-process cached) | none |
| `/api/ai-signal.toml` | GET | Live TOML manifest | none |
| `/api/health` | GET | `{ status, uptime, targetDir, lastManifestUpdate }` | none |
| `/api/state` | GET | Server-state envelope (Batch 032 QW-3) | none |
| `/api/manifests` | GET | Schema-card index + project healthScore (Batch 032 QW-3) | none |
| `/api/sonic/status` | GET | Sonic daemon lifecycle state (Batch 032) | none |
| `/api/files` | GET | Directory entries at `?path=` | none |
| `/api/index` | POST | Re-index a path | none |
| `/api/verify` | POST | Recompute SHA-256 across registry | none |
| `/api/file-intent` | POST | Upsert purpose/dependsOnBehavior/valueStatement | none |
| `/api/settings` | GET / POST | Settings CRUD | none |
| `/api/mutations` | GET | SSE event stream (mutation notifications) | none |
| `/api/concept-file` | POST | Register CONCEPT-phase file | none |
| `/api/mvp-lock` | POST | Bulk CONCEPT/DEVELOPMENT → LOCKED transition | none |
| `/api/production-promote` | POST | Purge dev mutation history for a fingerprint | none |
| `/api/prd` | GET | Generate PRD markdown | none |
| `/api/prd-projects` | GET / POST | PRD project records | none |
| `/api/prd-projects/:name` | GET | Fetch single PRD project | none |
| `/api/gap-analysis` | GET | GapAnalyzer report (JSON or text/markdown) | none |
| `/api/bruno-call` | POST | Lifecycle alarm pass | none |
| `/api/oscar-house` | POST | Lifecycle reaper pass | none |
| `/api/needs-ai-review` | GET | Files flagged for AI review | none |
| `/api/mark-reviewed` | POST | Mark a file as reviewed | none |
| `/api/templates` | GET / POST | PRD template management | none |
| `/api/signal-path` | GET / POST | Path-generator (FOUNDER P1) | none |
| `/api/generate-report` | POST | Report-generator (Markdown / JSON) | none |
| `/api/insights` | GET | Insight-store consumer | none |
| `/api/identity-risk` | GET | Identity-risk consumer (Wave 3C) | none |
| `/api/auth-token` | GET | Auth secret (loopback only) | loopback |
| `/api/record-commit` | POST | Git post-commit hook intake | X-St8-Secret |
| `/api/tickets` | GET / POST | Ticket CRUD (POST requires secret) | GET: none / POST: X-St8-Secret |
| `/api/tickets/count` | GET | Open ticket count | none |
| `/api/tickets/:id/claim` | POST | Claim a ticket | X-St8-Secret |
| `/api/tickets/:id/resolve` | POST | Resolve a ticket | X-St8-Secret |
| `/api/llm-call` | POST | Dispatch chat-completion to provider | X-St8-Secret |
| `/api/exec` | POST | Deliberately deferred — returns 501 | X-St8-Secret |

POST routes annotated `X-St8-Secret` require the header. Secret stored at `.st8/server.secret` (mode 0600). Frontend reads via `GET /api/auth-token` (loopback-gated) and uses `window.st8AuthFetch` wrapper.

Pre-Batch-032 drift corrected: this table previously listed `/api/state`, `/api/manifests`, `/api/events` (the last as 404; SSE actually lives at `/api/mutations`) along with 11 of the 36 actual routes. Batch 032 QW-3 implemented the first two and renamed the third to match implementation.

## Active sprint protocol

The 23-wave debugging sprint is documented in `st8_bible.md` (batches 028+).

Per-wave structure:
- **Executor agent** — runs 5-ish tickets in a single concern. Reads `PRE_FLIGHT.md`, runs pre-flight, executes, commits per-ticket, updates `docs/_pending-tickets/<cluster>.json`.
- **Reviewer agent** — audits the executor's commits. Updates verdicts in JSON. Appends to `docs/_pending-tickets/<cluster>.review.md`. Last reviewer in cluster renames JSON to `.for-review.json`.
- **Watchdog Monitor** — alerts on stalls (>15 min uncommitted-without-mtime-update OR >20 min wall-clock-since-last-commit).
- **Hard rule**: no extras per wave. Flags surfaced by reviewers become their own sub-wave.

## NEVER target these paths

- `src/0_*` — deleted historical skeleton
- `backend/`, `lib/` — deleted historical layout
- `/home/bozertron/Orchestr8_jr/` or any path outside `/home/user/st8/` (different project)
- `OGB/` — retired code, reference only

## When in doubt

- `git log -1 --format='%h %s'` to confirm what's actually HEAD
- `ls src/` to confirm canonical structure (must be: `core  features  frontend  shared`)
- `find src -maxdepth 2 -name "0_*"` to confirm no skeleton drift (must be empty)
- `cat st8-filemap.md` for the authoritative file inventory
